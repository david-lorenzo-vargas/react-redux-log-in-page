import React from 'react';
import styles from './input.scss';

const Input = (props) => {
  return (
    <div className={styles.input}>
      <input type="text" />
    </div>
  );
};

export default Input;
