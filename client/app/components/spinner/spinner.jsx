import React from 'react';
import styles from './spinner.scss';

const Spinner = () => (
  <div className={styles.spinner}>
    <div className={styles.spinner__item} />
  </div>
);

export default Spinner;
